# Deployment

Push the project to GitHub without `.env` or `venv/`. Deploy `app.py` on Streamlit Community Cloud. Add `GROQ_API_KEY` and `GROQ_MODEL` in Streamlit Secrets.

For a fixed interview dataset, keep the generated `output/employees.db` in the repository after running the pipeline locally.
